#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
print "Welcome to Tic Tac Toe!"
puts display_board(board)
puts "Where would you like to go?"
i = gets.strip
input_to_index(i)
move(board, input_to_index(i), value = "X")
display_board(board)
